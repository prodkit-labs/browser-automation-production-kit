services:
  worker:
    image: python:3.12-slim
    working_dir: /app
    volumes:
      - ../..:/app
    command: python -m prodkit_browser.jobs.docs_to_rag --fixture benchmarks/fixtures/docs_pages.json
