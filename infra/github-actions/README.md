# GitHub Actions

The repository workflow runs scheduled benchmark checks against local fixtures only.

Do not add provider credentials to public workflow files. Use repository secrets only after provider-backed benchmarks are reviewed.
